services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    restart: always