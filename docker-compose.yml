services:
  backend:
    image: xariif/engineering-work-backend:latest
    ports:
      - 5001
      - 5000
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: always

  frontend:
    image: xariif/engineering-work-frontend:latest
    ports:
      - 80
      - 3000
    restart: always